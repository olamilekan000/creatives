import React from 'react';

const SocialBtn = () => {
  return (
    <div className="social-btn">
      <div className="center">
        <button id="social-btn" className="pulse">
          Let's catch all
        </button>
      </div>
    </div>
  );
};

export default SocialBtn;
